name: Deploy CMS to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: mbstring, xml, ctype, iconv, intl, pdo_mysql, dom, filter, gd, json, zip
          tools: composer:v2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction

      - name: Install Node dependencies
        run: npm ci

      - name: Build frontend assets
        run: npm run build

      - name: Create production .env file
        run: |
          cat > .env << 'EOF'
          APP_NAME="Hyve CMS"
          APP_ENV=production
          APP_KEY=base64:+YpEhMO+1hz6NEwhbDVpZIe874CMBvfoHo/cvWWhdkU=
          APP_DEBUG=false
          APP_TIMEZONE=UTC
          APP_URL=https://palevioletred-snake-584520.hostingersite.com
          
          APP_LOCALE=en
          APP_FALLBACK_LOCALE=en
          APP_FAKER_LOCALE=en_US
          
          BCRYPT_ROUNDS=12
          
          LOG_CHANNEL=stack
          LOG_STACK=single
          LOG_DEPRECATIONS_CHANNEL=null
          LOG_LEVEL=error
          
          DB_CONNECTION=mysql
          DB_HOST=localhost
          DB_PORT=3306
          DB_DATABASE=u938549775_hyvecms
          DB_USERNAME=u938549775_hyvecms
          DB_PASSWORD="vC%&O^eCg3e9K5zx"
          
          SESSION_DRIVER=file
          SESSION_LIFETIME=120
          
          CACHE_STORE=file
          QUEUE_CONNECTION=sync
          
          FILESYSTEM_DISK=public
          EOF

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 156.67.67.67
          username: u938549775.palevioletred-snake-584520.hostingersite.com
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
          local-dir: ./
          server-dir: /public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/tests/**
            **/.env.example
            **/phpunit.xml

      - name: Run Migrations & Optimize on Production
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 156.67.67.67
          username: u938549775
          password: ${{ secrets.SSH_PASSWORD }}
          port: 65002
          script: |
            cd domains/palevioletred-snake-584520.hostingersite.com/public_html
            php artisan migrate --force
            php artisan optimize:clear
            php artisan config:cache
            php artisan route:cache
            php artisan view:cache
            echo "✅ Deployment completed successfully!"
